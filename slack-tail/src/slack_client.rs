use swagger::apis::*;
use swagger::models::*;
use swagger::apis::configuration::Configuration;

pub struct SlackClient {
}

impl SlackClient  {
    pub fn new() -> SlackClient {
        SlackClient {}
    }
}
